{{#each errors}}
    <div class="alert alert-danger">{{text}}</div>
{{else}}

{{/each}}
<h3>Edit category:</h3>
<div class="card">
    <div class="card-body">
        <form action="/admin/category/edit" method="post">
            <input type="hidden" name="id" value="{{category._id}}">
            <label for="name">Name: </label><br>
                <input type="text" id="name" name="name" placeholder="Name categary" class="form-control" value="{{category.name}}">
            <label for="slug">Slug: </label><br>
                <input type="text" id="slug" name="slug" placeholder="Name slug" class="form-control" value="{{category.slug}}"><br>
            <button type="submit" class="btn btn-success"> To Edit category</button>
        </form>
    </div>
</div>
